./guides-upload-from-a-url.1.html:<p>You can only suggest edits to Markdown body content, but not to the API spec.</p>
./classify.html:<p>This feature allows you to classify your videos based on a list of labels. Use the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">/classify</code> endpoint for a single video and the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">/classify/bulk</code> endpoint to classify all the videos within an index.</p>
./classify.html:<p>When invoking the PUT methods of the <a target="_self" href="classify-video.html"><button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">/classify</code></a> and <a target="_self" href="classify-bulk.html"><button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">/classify/bulk</code></a> endpoints you must specify a list of labels, and the API service will return the following, among other information:</p>
./quickstart.html:<p>This guide demonstrates how to build a production-ready search service by using a three-step process:</p>
./quickstart.html:<p>Note that each step is composed of a single API call.</p>
./quickstart.html:<p>This guide shows how to build an application that calls the Twelve Labs API using <a target="_self" href="https://www.python.org/">Python</a> and <a target="_self" href="https://nodejs.org/en/">Node.js</a>. However, note that the API  is compatible with most programming languages, and you can also use <a target="_self" href="https://www.postman.com/">Postman</a> or other REST clients to send requests and view responses.</p>
./quickstart.html:<p>A valid Twelve Labs account.</p>
./quickstart.html:<p>Note</p></h3><p>The Twelve Labs Video Understanding API is currently in closed beta. To create an account, you must first sign up for a demo. To do so, go to the <a target="_self" href="https://twelvelabs.typeform.com/to/kCxXwqzq?typeform-source=twelvelabs.io">Request a Demo</a> page and follow the instructions. You will receive a signup code that you can use to create an account.</p>
./quickstart.html:<p>This guide assumes you&#x27;re familiar with the concepts that are described on the <a target="_self" href="understand.html">Understand</a> page.</p>
./quickstart.html:<p>Your video must meet the following requirements:</p>
./quickstart.html:<p>Install the required packages. Depending on the programming language you use, enter one of the following commands:</p>
./quickstart.html:<p>For consistent search results, Twelve Labs recommends you upload 360p videos.</p>
./quickstart.html:<p>To make calls to the API, you must include your secret key, and specify the URL of the API. Twelve Labs recommends you use environment variables to pass configuration to your application. To retrieve your API key, go to the <a target="_self" href="https://api.twelvelabs.io/dashboard">Dashboard</a> page and select the copy button under the <strong>API Key</strong> section:</p>
./quickstart.html:<p> Then, enter the following commands, replacing the placeholder surrounded by <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">&lt;&gt;</code> with your API key:</p>
./quickstart.html:<p>An index groups one or more video vectors and is the most granular level at which you can perform a search. Follow the steps in the guide below to create an index.</p>
./quickstart.html:<p>Once you&#x27;ve created an index, you can upload a video. When the video has finished uploading, Twelve Labs&#x27; fully-managed video search engine will automatically create a rich set of vectors containing all the information the API service needs to perform fast, semantic, accurate, and scalable searches. Follow the steps in the guide below to upload a video.</p>
./quickstart.html:<p>When the API service has finished indexing the video, it can be searched by using the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">/search</code> endpoint. Follow the steps in the guide below to make a search request.</p>
./update-index.html:<p>This method updates the name of the specified index.</p>
./guides-upload-videos-cloud-to-cloud-transfers.html:<p>Cloud-to-cloud transfers allow you to upload multiple videos at once by grouping multiple video indexing operations in a single API call. Initially, this feature is supported for the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">us-west-2</code> region of AWS S3. Later, this feature will be extended to all the regions and other cloud providers. For details, see the <a target="_self" href="roadmap.html">Roadmap</a> page.</p>
./guides-upload-videos-cloud-to-cloud-transfers.html:<p>The steps for performing a cloud-to-cloud-transfer are as follows:</p>
./guides-upload-videos-cloud-to-cloud-transfers.html:<p>The topics below provide instructions for managing integrations and making cloud-to-cloud transfers.</p>
./guides-upload-videos-cloud-to-cloud-transfers.html:<p>An integration grants Twelve Labs access to read the files in your S3 bucket. Before you can use the cloud-to-cloud transfer feature, you must set up an integration by following the steps in this guide.</p>
./guides-upload-videos-cloud-to-cloud-transfers.html:<p>Open the <a target="_self" href="https://us-east-1.console.aws.amazon.com/iam/home#/home">IAM Dashboard </a>page.</p>
./guides-upload-videos-cloud-to-cloud-transfers.html:<p>On the <strong>Review Policy</strong> page, set the name of the policy to &quot;TwelvelabsIntegrationPolicy&quot; and enter a brief description. Then, select the <strong>Create Policy</strong> button:</p>
./guides-upload-videos-cloud-to-cloud-transfers.html:<p><span aria-label="" class="img" role="button" tabindex="0"><img src="https://files.readme.io/841ce5b-image.png" align="" alt="" caption="" height="auto" title="" width="auto" loading="lazy"/><span autofocus="" class="lightbox" role="dialog" tabindex="0"><span class="lightbox-inner"><img src="https://files.readme.io/841ce5b-image.png" align="" caption="" height="auto" title="Click to close..." width="auto" alt="" class="lightbox-img" loading="lazy"/></span></span></span></p>
./guides-upload-videos-cloud-to-cloud-transfers.html:<p>Note</p></h3><p>Do not change the name of the policy. If you rename the policy, the integration will not work.</p>
./guides-upload-videos-cloud-to-cloud-transfers.html:<p>On the <strong>Policies</strong> page, make sure that the system displays your new policy:</p>
./guides-upload-videos-cloud-to-cloud-transfers.html:<p><span aria-label="" class="img" role="button" tabindex="0"><img src="https://files.readme.io/89bb812-image.png" align="" alt="" caption="" height="auto" title="" width="auto" loading="lazy"/><span autofocus="" class="lightbox" role="dialog" tabindex="0"><span class="lightbox-inner"><img src="https://files.readme.io/89bb812-image.png" align="" caption="" height="auto" title="Click to close..." width="auto" alt="" class="lightbox-img" loading="lazy"/></span></span></span></p>
./guides-upload-videos-cloud-to-cloud-transfers.html:<p>If the system does not display your policy, review the steps in this section, making sure all the information you entered is correct.</p>
./guides-upload-videos-cloud-to-cloud-transfers.html:<p>On the<strong> Name, Review, and Create</strong> page, set the name of the role to <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">TwelvelabsIntegrationRole</code> and enter a brief description. Then, select the <strong>Create Role</strong> button:</p>
./guides-upload-videos-cloud-to-cloud-transfers.html:<p><span aria-label="" class="img" role="button" tabindex="0"><img src="https://files.readme.io/7bd7cda-image.png" align="" alt="" caption="" height="auto" title="" width="auto" loading="lazy"/><span autofocus="" class="lightbox" role="dialog" tabindex="0"><span class="lightbox-inner"><img src="https://files.readme.io/7bd7cda-image.png" align="" caption="" height="auto" title="Click to close..." width="auto" alt="" class="lightbox-img" loading="lazy"/></span></span></span></p>
./guides-upload-videos-cloud-to-cloud-transfers.html:<p>Note</p></h3><p>Do not change the name of the role. If you rename the role, the integration will not work.</p>
./guides-upload-videos-cloud-to-cloud-transfers.html:<p>On the <strong>Roles</strong> page, make sure that the system displays your new role:</p>
./guides-upload-videos-cloud-to-cloud-transfers.html:<p><span aria-label="" class="img" role="button" tabindex="0"><img src="https://files.readme.io/3d3846b-image.png" align="" alt="" caption="" height="auto" title="" width="auto" loading="lazy"/><span autofocus="" class="lightbox" role="dialog" tabindex="0"><span class="lightbox-inner"><img src="https://files.readme.io/3d3846b-image.png" align="" caption="" height="auto" title="Click to close..." width="auto" alt="" class="lightbox-img" loading="lazy"/></span></span></span></p>
./guides-upload-videos-cloud-to-cloud-transfers.html:<p>If the system does not display your role, review the steps in this section, making sure all the information you entered is correct.</p>
./guides-upload-videos-cloud-to-cloud-transfers.html:<p>Move to the Twelve Labs dashboard. Under <strong>Step 3 : Check Verification</strong>, select the <strong>Verify My Account</strong> button:</p>
./guides-upload-videos-cloud-to-cloud-transfers.html:<p><span aria-label="" class="img" role="button" tabindex="0"><img src="https://files.readme.io/eb48d89-select-verify-my-account.png" align="" alt="" caption="" height="auto" title="" width="auto" loading="lazy"/><span autofocus="" class="lightbox" role="dialog" tabindex="0"><span class="lightbox-inner"><img src="https://files.readme.io/eb48d89-select-verify-my-account.png" align="" caption="" height="auto" title="Click to close..." width="auto" alt="" class="lightbox-img" loading="lazy"/></span></span></span></p>
./guides-upload-videos-cloud-to-cloud-transfers.html:<p>If everything went well, you should see a message saying &quot;Verification successful!&quot;:</p>
./guides-upload-videos-cloud-to-cloud-transfers.html:<p><span aria-label="" class="img" role="button" tabindex="0"><img src="https://files.readme.io/65b489b-verification-successful.png" align="" alt="" caption="" height="auto" title="" width="auto" loading="lazy"/><span autofocus="" class="lightbox" role="dialog" tabindex="0"><span class="lightbox-inner"><img src="https://files.readme.io/65b489b-verification-successful.png" align="" caption="" height="auto" title="Click to close..." width="auto" alt="" class="lightbox-img" loading="lazy"/></span></span></span></p>
./guides-upload-videos-cloud-to-cloud-transfers.html:<p>On the <strong>Connected Integration</strong>s page, make sure that the status of your new integration shows as <strong>Active</strong>:</p>
./guides-upload-videos-cloud-to-cloud-transfers.html:<p><span aria-label="" class="img" role="button" tabindex="0"><img src="https://files.readme.io/279a194-image.png" align="" alt="" caption="" height="auto" title="" width="auto" loading="lazy"/><span autofocus="" class="lightbox" role="dialog" tabindex="0"><span class="lightbox-inner"><img src="https://files.readme.io/279a194-image.png" align="" caption="" height="auto" title="Click to close..." width="auto" alt="" class="lightbox-img" loading="lazy"/></span></span></span></p>
./guides-upload-videos-cloud-to-cloud-transfers.html:<p>When the status of your integration shows as <strong>Active</strong>, it means that the API service can access the videos in your S3 bucket. If the status does not show as <strong>Active</strong>,  review the steps in this section, making sure all the information you entered is correct.</p>
./guides-upload-videos-cloud-to-cloud-transfers.html:<p>The <a target="_self" href="https://api.twelvelabs.io/integrations">Integrations</a> page allows you to download the list of files in your AWS S3 bucket as a JSON. The structure of the JSON file is described on the <a target="_self" href="tasks-transfers.html">Tasks/Transfers</a> page. Once you&#x27;ve downloaded the JSON file, you can edit the fields in a plain text editor. For example, you can add transcription files or remove the files you don&#x27;t want to upload and index.</p>
./guides-upload-videos-cloud-to-cloud-transfers.html:<p><em>(Optional)</em> You can edit your JSON file in a plain-text editor. For example, you can provide your custom transcription files, as shown in the following example:</p>
./guides-upload-videos-cloud-to-cloud-transfers.html:<p>This guide shows how to transfer the files from your AWS S3 bucket to Twelve Labs and index them.</p>
./guides-upload-videos-cloud-to-cloud-transfers.html:<p>Note</p></h3><p>The API service checks for duplicate files using hashes and will not upload the same video to the same index twice.</p>
./guides-upload-videos-cloud-to-cloud-transfers.html:<p>Declare the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">/indexes/tasks/transfers</code> endpoint:</p>
./guides-upload-videos-cloud-to-cloud-transfers.html:<p>Read your JSON file. Open a stream making sure to replace the placeholders surrounded by <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">&lt;&gt;</code> with your values:</p>
./guides-upload-videos-cloud-to-cloud-transfers.html:<p>If you&#x27;re using Python, store the file to upload in an array named <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">file_param</code> and specify that you want to make a <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">multipart/form-data</code> request. If you&#x27;re using Node.js, store the file to upload in a variable named <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">formData</code> of type <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">FormData</code>:</p>
./guides-upload-videos-cloud-to-cloud-transfers.html:<p>Upload the JSON file. Call the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">/indexes/tasks/transfer</code> endpoint, store the result in a variable named <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">response</code>, and print the status code and response:</p>
./guides-upload-videos-cloud-to-cloud-transfers.html:<p>Follow the steps in the sections below to view statistics, synchronize data, and remove integrations.</p>
./guides-upload-videos-cloud-to-cloud-transfers.html:<p>You can use the dashboard to retrieve the following statistics about an S3 bucket</p>
./guides-upload-videos-cloud-to-cloud-transfers.html:<p>You can use this information to make decisions about initiating a new indexing task.</p>
./guides-upload-videos-cloud-to-cloud-transfers.html:<p>To view statistics, go to the <a target="_self" href="https://api.twelvelabs.io/integrations">Integrations</a> page, choose a bucket, and select the down arrow located at the end of the row. The following example screenshot shows a bucket that contains three files:</p>
./guides-upload-videos-cloud-to-cloud-transfers.html:<p><span aria-label="" class="img" role="button" tabindex="0"><img src="https://files.readme.io/5bc9ec2-view-statistics.png" align="" alt="" caption="" height="auto" title="" width="auto" loading="lazy"/><span autofocus="" class="lightbox" role="dialog" tabindex="0"><span class="lightbox-inner"><img src="https://files.readme.io/5bc9ec2-view-statistics.png" align="" caption="" height="auto" title="Click to close..." width="auto" alt="" class="lightbox-img" loading="lazy"/></span></span></span></p>
./guides-upload-videos-cloud-to-cloud-transfers.html:<p>Synchronizing data refers to the process of data being propagated from your cloud storage to the API service. When adding, modifying, or removing files, the changes are not automatically propagated, and you must initiate a synchronization.</p>
./guides-upload-videos-cloud-to-cloud-transfers.html:<p>To initiate a synchronization, go to the <a target="_self" href="https://api.twelvelabs.io/integrations">Integrations</a> page, choose a bucket, and select the <strong>Synchronize Data</strong> icon which is the second from the left:</p>
./guides-upload-videos-cloud-to-cloud-transfers.html:<p><span aria-label="" class="img" role="button" tabindex="0"><img src="https://files.readme.io/fbe0ad8-select-synchronize-data-icon.png" align="" alt="" caption="" height="auto" title="" width="auto" loading="lazy"/><span autofocus="" class="lightbox" role="dialog" tabindex="0"><span class="lightbox-inner"><img src="https://files.readme.io/fbe0ad8-select-synchronize-data-icon.png" align="" caption="" height="auto" title="Click to close..." width="auto" alt="" class="lightbox-img" loading="lazy"/></span></span></span></p>
./guides-upload-videos-cloud-to-cloud-transfers.html:<p>Note</p></h3><p>The API service automatically initiates a synchronization when you select the <strong>Get JSON</strong> button.</p>
./guides-upload-videos-cloud-to-cloud-transfers.html:<p>When a specific integration is no longer needed, you can remove it.</p>
./guides-upload-videos-cloud-to-cloud-transfers.html:<p>To remove one or more integrations, go to the <a target="_self" href="https://api.twelvelabs.io/integrations">Integrations</a> page and select the checkbox at the left of the row for all integrations you want to delete. Then, select the <strong>Remove</strong> icon:</p>
./guides-upload-videos-cloud-to-cloud-transfers.html:<p><span aria-label="" class="img" role="button" tabindex="0"><img src="https://files.readme.io/6f5f087-delete-integration.png" align="" alt="" caption="" height="auto" title="" width="auto" loading="lazy"/><span autofocus="" class="lightbox" role="dialog" tabindex="0"><span class="lightbox-inner"><img src="https://files.readme.io/6f5f087-delete-integration.png" align="" caption="" height="auto" title="Click to close..." width="auto" alt="" class="lightbox-img" loading="lazy"/></span></span></span></p></div><div id="updated-at"><p class="DateLine "><i class="icon icon-watch"></i>Updated<!-- --> <!-- -->3 days ago<!-- --> </p>
./guides-providing-transcriptions-from-urls.html:<p>In this guide you&#x27;ll learn how to upload a video file and provide a transcription when both files are available from public URLs. The API service will retrieve the files directly from the specified URLs, so your application doesn&#x27;t have to store them locally and upload the bytes.</p>
./guides-providing-transcriptions-from-urls.html:<p>Note that, although the example code in this guide is written in <a target="_self" href="https://www.python.org/">Python</a> and <a target="_self" href="https://nodejs.org/en/">Node.js</a> the API  is compatible with most programming languages, and you can also use <a target="_self" href="https://www.postman.com/">Postman</a> or other REST clients to send requests and view responses.</p>
./guides-providing-transcriptions-from-urls.html:<p>Declare the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">/tasks</code> endpoint:</p>
./guides-providing-transcriptions-from-urls.html:<p>To provide a transcription file, you must set the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">provide_transcription</code> parameter to <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">true</code>, and specify the index ID, the language of your video, and the URLs of your video and transcription file. If you&#x27;re using Python, declare a dictionary named <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">data</code> and use it to store all the required parameters. If you&#x27;re using Node.js, declare a variable named <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">formData</code> of type <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">FormData</code> and use it to store all the required parameters.</p>
./guides-providing-transcriptions-from-urls.html:<p>Upload your video. Call the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">/tasks</code> endpoint and store the result in a variable named <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">response</code>:</p>
./guides-providing-transcriptions-from-urls.html:<p>Store the ID of your task in a variable named <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">TASK_ID</code> and print the status code and the response:</p>
./guides-providing-transcriptions-from-urls.html:<p><em>(Optional) </em> You can use the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">/tasks/{_id}</code> endpoint to monitor the indexing process. Wait until the status shows as <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">ready</code>:</p>
./guides-providing-transcriptions-from-urls.html:<p>The output should look similar to the following one:</p>
./guides-providing-transcriptions-from-urls.html:<p>Note</p></h3><p>For details about the possible statuses, see the <a target="_self" href="tasks.html">Tasks</a> page.</p></blockquote></div><div id="updated-at"><p class="DateLine "><i class="icon icon-watch"></i>Updated<!-- --> <!-- -->19 days ago<!-- --> </p>
./guides-requirements-for-processing-notifications.1.html:<p>You can only suggest edits to Markdown body content, but not to the API spec.</p>
./guides-upload-from-the-local-file-system.1.html:<p>You can only suggest edits to Markdown body content, but not to the API spec.</p>
./guides-providing-your-custom-transcriptions.html:<p>If an index has the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">conversation</code> option enabled, the API service automatically extracts a transcription. However, if you already have a transcription file, you can provide it along with the video when requesting a video indexing task. Note that transcriptions must be in the SRT or VTT format.</p>
./guides-providing-your-custom-transcriptions.html:<p>Depending on how you want to upload your videos and provide transcriptions, proceed to one of the following sections:</p>
./concepts-filtering.html:<p>All Twelve Labs API endpoints that return a list of results support filtering, but the implementation is slightly different depending on whether you query the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">/indexes</code>  endpoint or want to filter your search results based on metadata.</p>
./concepts-filtering.html:<p>Depending on your use case, proceed to one of the following sections:</p>
./indexes.html:<p>An index groups one or more videos and is the most granular level at which you can perform a search. The <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">index</code> object has the following elements:</p>
./combined-queries-retrieve-specific-page.html:<p>Use this endpoint to retrieve a specific page of search results.</p>
./combined-queries-retrieve-specific-page.html:<p><strong>NOTES</strong>:</p>
./guides-extracting-information-from-videos.html:<p>Videos contain combinations of images, spoken words, sounds, movements, and text. When you have a large library of videos, it might be difficult to know what&#x27;s in your videos and how to write a query that finds the exact moments you need. Although the API service doesn&#x27;t automatically extract any tags from your videos and doesn&#x27;t require you to use them when you perform searches, extracting information that appears in a video can help you get insights into the content of your videos. Instead of watching the entire video and annotating it manually, you can use information extracted by the API service to perform more fine-grained searches.</p>
./guides-extracting-information-from-videos.html:<p>Depending on your use case, proceed to one of the following sections:</p>
./concepts-pagination.1.html:<p>You can only suggest edits to Markdown body content, but not to the API spec.</p>
./understand.html:<p>Twelve Labs Video Understanding API is a cloud-based, AI-powered video search service offering full natural language-based search. The API is organized around REST, and it allows you to integrate more than twenty state-of-the-art (”SOTA”) deep learning models that extract information from your videos and make it available to your applications. The API is compatible with most programming languages, and you can also use Postman or other REST clients to send requests and view responses.</p>
./understand.html:<p>The following diagram illustrates the architecture of Twelve Labs Video Understanding API:</p>
./understand.html:<p>Architecture overview of Twelve Labs Video Understanding API</p>
./understand.html:<p>The video search service is composed of the following main components:</p>
./understand.html:<p>The following diagram illustrates on a high level the workflow of using the API:</p>
./understand.html:<p><span aria-label="" class="img" role="button" tabindex="0"><img src="https://files.readme.io/db14f47-_2022-01-14__6.20.24.png" align="" alt="" caption="" height="auto" title="" width="auto" loading="lazy"/><span autofocus="" class="lightbox" role="dialog" tabindex="0"><span class="lightbox-inner"><img src="https://files.readme.io/db14f47-_2022-01-14__6.20.24.png" align="" caption="" height="auto" title="Click to close..." width="auto" alt="" class="lightbox-img" loading="lazy"/></span></span></span></p>
./understand.html:<p>The steps are as follows:</p>
./guides-using-webhooks.html:<p>A webhook allows your application to receive real-time HTTP notifications for events. Currently, this functionality is available for the following events:</p>
./guides-using-webhooks.html:<p>To receive event notifications for events in your application, use the steps in this section:</p>
./guides-performing-searches.1.html:<p>You can only suggest edits to Markdown body content, but not to the API spec.</p>
./update-video-information.html:<p>Use this method to update the title and the metadata of a video.</p>
./guides-webhooks-notification-schema.html:<p>The header contains a field named <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">TL-Signature</code>. This allows you to verify that the event was sent by Twelve Labs. The <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">TL-Signature</code> field is composed of two parts:</p>
./guides-webhooks-notification-schema.html:<p>Responses are in JSON format and contain the following fields:</p>
./guides-authentication.1.html:<p>You can only suggest edits to Markdown body content, but not to the API spec.</p>
./list-search-engines.html:<p>This method lists all the search engines that are available in your account.</p>
./retrieve-thumbnail.html:<p>To use this feature, you must enable thumbnail generation for the index with which the video is associated. For details, see the <a target="_self" href="https://docs.twelvelabs.io/reference/createindex">Create an index</a> section.</p>
./index.html:<p>Twelve Labs Video Understanding API is an AI-powered video search solution that extracts information such as movement, objects, sound, text on screen, and speech from your videos and allows you to find specific moments using everyday language.</p>
./index.html:<p>The following sections describe the key capabilities of Twelve Labs Video Understanding API and compare it to other solutions.</p>
./index.html:<p>Developers using Twelve Labs Video Understanding API have access to the following key capabilities:</p>
./index.html:<p>Note</p></h3><p>Twelve Labs is continuously adding new features. See the <a target="_self" href="roadmap.html">Roadmap</a> page for details.</p>
./index.html:<p>The table below provides a basic comparison between Twelve Labs Video Understanding API and other cloud-based solutions:</p>
./index.html:<p>Twelve Labs Video Understanding API compared to other solutions</p>
./guides-single-search-option.html:<p>This section shows how to perform searches by specifying a single search option. A search option represents the source of information the search engine uses when performing a search. For details, see the<a target="_self" href="concepts-search-options.html"> Search options</a> page.</p>
./guides-single-search-option.html:<p>Note that, for each matching video fragment, the API service returns, among other information, a field named <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">metadata</code>. It is an array of objects where each object contains details about the type of search that matched your search terms and is composed of the following key-value pairs:</p>
./guides-single-search-option.html:<p>Example:</p>
./guides-single-search-option.html:<p>For a description of each field in the response, see the <a target="_self" href="make-search-request.html">Make a search request</a> page.</p>
./guides-single-search-option.html:<p>The examples in the following sections assume the following:</p>
./guides-single-search-option.html:<p>Note that, although the example code in this guide is written in <a target="_self" href="https://www.python.org/">Python</a> and <a target="_self" href="https://nodejs.org/en/">Node.js</a>, the API  is compatible with most programming languages, and you can also use <a target="_self" href="https://www.postman.com/">Postman</a> or other REST clients to send requests and view responses.</p>
./guides-single-search-option.html:<p>The API service allows you to search by objects, actions, sounds, movements, places, situational events, and a complex audio-visual text description. The following example searches for car accidents using visual cues:</p>
./guides-single-search-option.html:<p>The following example output was truncated for brevity:</p>
./guides-single-search-option.html:<p>The API service extracts a transcript from the video and performs an NLP analysis on the transcript. This allows you to find the exact point in your video where the specified word or phrase is mentioned.</p>
./guides-single-search-option.html:<p>Note that you can perform both semantic searches (the API service assesses the meaning and context) and exact searches (the API service searches for exact matches of your search terms).</p>
./guides-single-search-option.html:<p>To specify the type of search you want to perform, you can use the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">conversation_option</code> parameter. If not specified, the default value is <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">semantic</code>.</p>
./guides-single-search-option.html:<p>The following example performs a semantic search and returns the matches for the specified search term - &quot;car accidents&quot;:</p>
./guides-single-search-option.html:<p>The following example output was truncated for brevity:</p>
./guides-single-search-option.html:<p>The API service performs a text recognition (OCR) of each frame. This allows you to search for text that appears in the video. The following example searches for text that matches &quot;bear cubs&quot;.</p>
./guides-single-search-option.html:<p>The output should look similar to the following one:</p>
./guides-single-search-option.html:<p>This endpoint supports pagination. For details, see the <a target="_self" href="pagination-search.html">Pagination &gt; Search</a> page.</p>
./guides-single-search-option.html:<p>This endpoint supports filtering. For details, see the <a target="_self" href="concepts-filtering-search-results.html">Filter search results based on metadata</a> page.</p>
./guides-single-search-option.html:<p>This endpoint allows you to group or ungroup your search results based on the unique identifiers of the videos. For details, see the <a target="_self" href="grouping-and-ungrouping-items-in-a-response.html">Grouping and ungrouping items in a response</a> page.</p>
./create-video-indexing-task.html:<p>Use this method to create a new video indexing task that uploads a video to the API service and makes it searchable. Note the following about creating a new video indexing task:</p>
./concepts-indexing-options.1.html:<p>You can only suggest edits to Markdown body content, but not to the API spec.</p>
./tasks.html:<p>A task represents a request to upload and index a video. A task starts when you begin uploading your video by calling the POST method of the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">/tasks</code> endpoint, and it can be in one of the following statuses:</p>
./tasks.html:<p>The <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">task</code> object has the following properties:</p>
./tasks.html:<p>Note</p></h3><p>When using this endpoint, you can set up webhooks to receive notifications. For details, see the <a target="_self" href="guides-using-webhooks.html">Using webhooks</a> section.</p>
./guides-extracting-transcriptions.1.html:<p>You can only suggest edits to Markdown body content, but not to the API spec.</p>
./guides-uploading-videos.1.html:<p>You can only suggest edits to Markdown body content, but not to the API spec.</p>
./delete-video-information.html:<p>This method deletes all the information about the specified video This action cannot be undone.</p>
./guides-upload-from-the-local-file-system.html:<p>In this guide you&#x27;ll learn how to upload a video file from the local file system to the API service.</p>
./guides-upload-from-the-local-file-system.html:<p>Note that, although the example code in this guide is written in <a target="_self" href="https://www.python.org/">Python</a> and <a target="_self" href="https://nodejs.org/en/">Node.js</a>, the API  is compatible with most programming languages, and you can also use <a target="_self" href="https://www.postman.com/">Postman</a> or other REST clients to send requests and view responses.</p>
./guides-upload-from-the-local-file-system.html:<p>Declare the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">/tasks</code> endpoint:</p>
./guides-upload-from-the-local-file-system.html:<p>Read your video file. Open a stream, making sure to replace the placeholders surrounded by <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">&lt;&gt;</code> with your values:</p>
./guides-upload-from-the-local-file-system.html:<p>If you&#x27;re using Python, store the index ID and the language of your video in a dictionary named <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">data</code> and the file to upload in an array named <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">file_param</code>. If you&#x27;re using Node.js, store the index, the language of the video, and the file to upload in a variable named <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">formData</code> of type <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">FormData</code>:</p>
./guides-upload-from-the-local-file-system.html:<p>Upload your video. Call the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">/tasks</code> endpoint and store the result in a variable named <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">response</code>:</p>
./guides-upload-from-the-local-file-system.html:<p>Store the ID of your task in a variable named <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">TASK_ID</code> and print the status code and response:</p>
./guides-upload-from-the-local-file-system.html:<p>The output should look similar to the following one:</p>
./guides-upload-from-the-local-file-system.html:<p><em>(Optional)</em> You can use the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">/tasks/{_id}</code> endpoint to monitor the indexing process. Wait until the status shows as <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">ready</code>:</p>
./guides-upload-from-the-local-file-system.html:<p><em>(Optional)</em>  Store the unique identifier of your video in a variable named <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">VIDEO_ID</code>:</p>
./guides-upload-from-the-local-file-system.html:<p><em>(Optional)</em>  Print the unique identifier of your video, the status code, and the response:</p>
./guides-upload-from-the-local-file-system.html:<p>The output should look similar to the following one:</p>
./guides-upload-from-the-local-file-system.html:<p>Notes</p>
./delete-index.html:<p>This method deletes the specified index and all the videos within it. This action cannot be undone.</p>
./guides-multiple-search-options.1.html:<p>You can only suggest edits to Markdown body content, but not to the API spec.</p>
./versioning.html:<p>The sections below list all new features, enhancements, and changes to the API service, in chronological order.</p>
./versioning.html:<p>The introduction of new features and improvements in the v1.1 version of the Twelve Labs Video Understanding API has required changes to some endpoints. If you have used version v1.0 of the API, please refer to the following sections for important information regarding the changes.</p>
./versioning.html:<p>Version 1.1 of the Twelve Labs Video Understanding API introduces the following new features:</p>
./versioning.html:<p>This section lists the differences between version 1 and version 1.1 of the Twelve Labs Video Understanding API.</p>
./versioning.html:<p>The following methods now return a <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">200 OK</code> status code when the response is empty:</p>
./versioning.html:<p>The <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">/tasks</code> endpoint is now a separate endpoint and is no longer part of the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">/indexes</code> endpoint. The table below shows the changes made to each method of the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">/tasks</code> endpoint:</p>
./versioning.html:<p>The <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">/indexes/tasks/{task_id}/video_id</code> endpoint has been deprecated. You can now retrieve the unique identifier of a video by invoking the GET method of the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">/tasks/{task_id}</code> endpoint. The response will contain a field named <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">video_id</code>. For details, see steps six and seven on the <a target="_self" href="guides-upload-from-the-local-file-system.html">Upload from the local file system</a> page.</p>
./versioning.html:<p>When an error occurs, the API service now follows the recommendations of the <a target="_self" href="https://www.rfc-editor.org/rfc/rfc9110.html">RFC 9110</a> standard. Instead of numeric codes, the API service now returns string values containing human-readable descriptions of the errors. The format of the error messages is as follows:</p>
./versioning.html:<p>Now, when using the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">1.1</code> version of the API, the error should look similar to the following one:</p>
./versioning.html:<p>For a list of error messages, see the <a target="_self" href="error-codes.html">API Reference &gt; Error codes</a> page.</p>
./versioning.html:<p>The <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">next_page_id</code> and <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">prev_page_id</code> fields of the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">page_info</code> object have been renamed to <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">next_page_token</code> and <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">prev_page_id.</code></p>
./versioning.html:<p>The <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">type</code> field has been removed from all the responses.</p>
./versioning.html:<p>When performing searches specifying multiple search options, the API service returns an object containing the confidence level that a specific video fragment matched your search terms for each type of search. In version <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">v1.0</code>, this field was a dictionary named <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">module_confidence</code>.  In version <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">v1.1</code>, this field is now named <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">module</code> and is of type <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">array</code>. For details, see the <a target="_self" href="guides-multiple-search-options.html">Multiple search Options</a> page.</p>
./versioning.html:<p>The POST method of the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">/search/{page-token}</code> endpoint has been deprecated. To retrieve the subsequent pages, you must now call the GET method of the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">/search/{page-token}</code> endpoint, passing it the unique identifier of the page you want to retrieve. For details, see the <a target="_self" href="pagination-search.html">Pagination &gt; Search</a> page.</p>
./create-index.html:<p>This method creates an index.</p>
./retrieve-search-engine.html:<p>This method retrieves a search engine.</p>
./conversation-options.html:<p>When making a search request, use the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">conversation_option</code> parameter to specify the type of search the API service should perform. This parameter can take the following values:</p>
./pagination-indexes.html:<p>The <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">GET</code> methods of the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">/indexes</code>, <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">/indexes/videos</code>, and <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">/tasks</code>  endpoints support the following query parameters that allow you to control pagination behavior:</p>
./pagination-indexes.html:<p>To separate the endpoint and the query parameters, you must use a <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">?</code> symbol. If you add multiple query parameters, you must separate them using the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">&amp;</code> symbol as shown in this example: <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">page=2&amp;page_limit=5</code>.</p>
./pagination-indexes.html:<p>To paginate through results, begin with a <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">page</code> value of <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">1</code> and a <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">page_limit</code> value of <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">X</code>. To retrieve the next page, set the page parameter to <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">2</code>, while the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">page_limit</code>  parameter remains the same. To retrieve the rest of the pages, you must subsequently increment the value of the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">page</code> parameter by <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">1</code>.</p>
./pagination-indexes.html:<p>Each response contains, among other information, the items on the page you requested, the page you retrieved, and the total number of pages. When the page you&#x27;ve retrieved is equal to the total number of pages, you&#x27;ve reached the end of the dataset.</p>
./pagination-indexes.html:<p>Note</p>
./pagination-indexes.html:<p>The examples in this section are specific to using the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">/tasks</code> endpoint. However, the principles demonstrated are similar when using the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">/indexes</code> and <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">/indexes/videos</code> endpoints.</p>
./pagination-indexes.html:<p>The examples in this section assume you&#x27;re familiar with the concepts that are described in the <a target="_self" href="quickstart.html">Quickstart</a> section.</p>
./pagination-indexes.html:<p>Note that, although the example code in this guide is written in <a target="_self" href="https://www.python.org/">Python</a> and <a target="_self" href="https://nodejs.org/en/">Node.js</a> the API  is compatible with most programming languages, and you can also use <a target="_self" href="https://www.postman.com/">Postman</a> or other REST clients to send requests and view responses.</p>
./pagination-indexes.html:<p>The following example code retrieves the first page of results using the default value for the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">page</code> and <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">page_limit</code> parameters by calling the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">GET</code> method of the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">/indexes/tasks</code> endpoint with no parameters:</p>
./pagination-indexes.html:<p>The following example output was truncated for brevity:</p>
./pagination-indexes.html:<p>In this example output, note the following about the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">page_info</code> object:</p>
./pagination-indexes.html:<p>The following example code uses the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">page</code> query parameter to retrieve a specific page (<button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">2</code>):</p>
./pagination-indexes.html:<p>The following example output was truncated for brevity:</p>
./pagination-indexes.html:<p>In this example output, note that the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">page</code>property of the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">page_info</code> object shows that you&#x27;ve retrieved the second page.</p>
./pagination-indexes.html:<p>The following example code uses the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">page_limit</code> query parameter to specify a limit of <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">5</code> items on each page:</p>
./pagination-indexes.html:<p>The following example output was truncated for brevity:</p>
./pagination-indexes.html:<p>In this example output, note that the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">page</code> property of the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">page_info</code> object shows that the API returned <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">5</code> items per page.</p></div><div id="updated-at"><p class="DateLine "><i class="icon icon-watch"></i>Updated<!-- --> <!-- -->18 days ago<!-- --> </p>
./classify-bulk.html:<p>Use this method to classify all the videos in the specified index based on the entities or actions shown in those videos. Note that this endpoint returns the first page or results. To retrieve the rest of the pages, you must call the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">/classify/{page_token}</code> endpoint passing it the token that identifies the page you want to retrieve.</p>
./guides-upload-from-a-url.html:<p>In this guide you&#x27;ll learn how to upload a video file from a publicly accessible URL. The API service will retrieve the file directly from the specified URL, so your application doesn&#x27;t have to store the video locally and upload the bytes.</p>
./guides-upload-from-a-url.html:<p>Note that, although the example code in this guide is written in <a target="_self" href="https://www.python.org/">Python</a> and <a target="_self" href="https://nodejs.org/en/">Node.js</a>, the API  is compatible with most programming languages, and you can also use <a target="_self" href="https://www.postman.com/">Postman</a> or other REST clients to send requests and view responses.</p>
./guides-upload-from-a-url.html:<p>Declare the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">/tasks</code> endpoint:</p>
./guides-upload-from-a-url.html:<p>If you&#x27;re using Python, declare a dictionary named <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">data</code> and use it to store the index ID, language, and the URL of your video. If you&#x27;re using Node.js, declare a variable named <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">formData</code> of type <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">FormData</code> and use it to store the index ID, language, and the URL of your video:</p>
./guides-upload-from-a-url.html:<p>Upload your video. Call the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">/tasks</code> endpoint and store the result in a variable named <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">response</code>:</p>
./guides-upload-from-a-url.html:<p>Store the ID of your task in a variable named <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">TASK_ID</code> and print the status code and response:</p>
./guides-upload-from-a-url.html:<p>The output should look similar to the following one:</p>
./guides-upload-from-a-url.html:<p><em>(Optional)</em> You can use the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">/tasks/{_id}</code> endpoint to monitor the indexing process. Wait until the status shows as <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">ready</code>:</p>
./guides-upload-from-a-url.html:<p><em>(Optional)</em> Store the unique identifier of your video in a variable named <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">VIDEO_ID</code>:</p>
./guides-upload-from-a-url.html:<p><em>(Optional)</em> Print the unique identifier of your video, the status code, and the response:</p>
./guides-upload-from-a-url.html:<p>The output should look similar to the following one:</p>
./guides-upload-from-a-url.html:<p>Notes</p>
./list-video-indexing-tasks.html:<p>This method returns a list of the video indexing tasks in your account. The API returns your video indexing tasks sorted by creation date, with the newest at the top of the list.</p>
./guides-combined-queries.html:<p>Note</p></h3><p>The combined queries feature is in beta release and is subject to change.</p>
./guides-combined-queries.html:<p>A combined query includes any number of search queries linked with any number of logical operators. Combined queries are executed in one API request and offer the following main benefits:</p>
./guides-combined-queries.html:<p>Although the example code in this guide is written in <a target="_self" href="https://www.python.org/">Python</a> and <a target="_self" href="https://nodejs.org/en/">Node.js</a>, the API  is compatible with most programming languages, and you can also use <a target="_self" href="https://www.postman.com/">Postman</a> or other REST clients to send requests and view responses.</p>
./guides-combined-queries.html:<p>The examples in the following sections assume the following:</p>
./guides-combined-queries.html:<p>In its simplest form, the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">query</code> object is composed of the following key-value pairs:</p>
./guides-combined-queries.html:<p>The following example searches for car accidents based on visual cues:</p>
./guides-combined-queries.html:<p>The following output was truncated for brevity:</p>
./guides-combined-queries.html:<p>For a description of each field in the response, see the <a target="_self" href="combined-queries-make-search-request.html">Make a search request</a> page.</p>
./guides-combined-queries.html:<p>The API service allows you to build combined queries using the following logical operators:</p>
./guides-combined-queries.html:<p>The following is an example of a combined query that finds the points in your videos where a blue or a red car appears:</p>
./guides-combined-queries.html:<p>The following is an example of a combined query that finds the points in your video where:</p>
./guides-combined-queries.html:<p>This section illustrates some advanced techniques you can use to extend the functionality of complex queries.</p>
./guides-combined-queries.html:<p>Note that the following parameters can only be used when your combined query is composed of two or more  queries:</p>
./guides-combined-queries.html:<p>When building combined queries, you can use the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">adjacency_threshold</code> parameter to extend the lower and upper boundaries of each matching video fragment. </p>
./guides-combined-queries.html:<p>The following example code uses the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">AND</code> logical operator and the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">adjaceancy_threshold</code> parameter to find all car accidents that happened within 30 seconds before someone wins a race:</p>
./guides-combined-queries.html:<p>This section shows how you can convert your query to the new format. For example, the code that uses the old format to find the exact moments when a red car appears or is mentioned in the conversation should look similar to the following example:</p>
./guides-combined-queries.html:<p>To use the new format, the code should be updated as follows:</p>
./guides-combined-queries.html:<p>This endpoint supports pagination. For details, see the <a target="_self" href="pagination-search.html">Pagination &gt; Search</a> page.</p>
./guides-combined-queries.html:<p>This endpoint supports filtering. For details, see the <a target="_self" href="concepts-filtering-search-results.html">Filter search results based on metadata</a> page.</p>
./conversation-options.1.html:<p>You can only suggest edits to Markdown body content, but not to the API spec.</p>
./pagination-indexes.1.html:<p>You can only suggest edits to Markdown body content, but not to the API spec.</p>
./guides-providing-your-custom-transcriptions.1.html:<p>You can only suggest edits to Markdown body content, but not to the API spec.</p>
./combined-queries-make-search-request.html:<p>Use the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">/beta/search</code> endpoint to search for relevant matches in an index using combined queries.</p>
./combined-queries-make-search-request.html:<p>This endpoint returns the first page of results. To retrieve the subsequent pages, you must call the GET method of the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">/beta/search/{page-token}</code> endpoint, passing it the unique identifier of the page you want to retrieve.</p>
./combined-queries-make-search-request.html:<p><strong>NOTES</strong>:</p>
./retrieve-transcription.html:<p>This method retrieves a transcription of the spoken words.</p>
./grouping-and-ungrouping-items-in-a-response.1.html:<p>You can only suggest edits to Markdown body content, but not to the API spec.</p>
./pagination-search.html:<p>The <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">/search</code> endpoint supports the following parameters that allow you to control pagination behavior:</p>
./pagination-search.html:<p>When making the initial request, you call the POST method of the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">/search</code> endpoint, passing it a page_limit value of <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">X</code>. The response will contain, among other information, the first page of results, the identifier of the next page, and the time when the page expires. To retrieve the rest of the pages, invoke the GET method of the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">/search/{page-token}</code> endpoint, replacing the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">{page-token}</code> path parameter with the identifier of the page that you want to retrieve. When the API does not return the identifier of the next page, you&#x27;ve reached the end of the dataset.</p>
./pagination-search.html:<p>Note</p>
./pagination-search.html:<p>Note that, although the example code in this guide is written in  <a target="_self" href="https://www.python.org/">Python</a>  and  <a target="_self" href="https://nodejs.org/en/">Node.js</a>  the API is compatible with most programming languages, and you can also use  <a target="_self" href="https://www.postman.com/">Postman</a>  or other REST clients to send requests and view responses.</p>
./pagination-search.html:<p>The following example code retrieves the first page of results by calling the POST method of the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">/search</code> endpoint:</p>
./pagination-search.html:<p>The following example output was truncated for brevity:</p>
./pagination-search.html:<p>In this example output, note the following about the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">page_info</code> object :</p>
./pagination-search.html:<p>The following example code retrieves a specific page by calling the GET method of the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">/search</code> endpoint, replacing the placeholder surrounded by <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">&lt;&gt;</code> with the identifier of the page you want to retrieve:</p>
./pagination-search.html:<p>The following example output was truncated for brevity:</p>
./pagination-search.html:<p>In this example output, note the following about the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">page_info</code> object :</p>
./pagination-search.html:<p>The following example code specifies a limit of 3 items on each page using the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">page_limit</code> parameter:</p>
./pagination-search.html:<p>The output should look similar to the following one:</p>
./pagination-search.html:<p>In this example output, note that the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">limit_per_page</code> property of the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">page_info</code> object shows that the API service returned three items per page.</p></div><div id="updated-at"><p class="DateLine "><i class="icon icon-watch"></i>Updated<!-- --> <!-- -->8 days ago<!-- --> </p>
./search-retrieve-specific-page.html:<p>Use this endpoint to retrieve a specific page of search results.</p>
./search-retrieve-specific-page.html:<p><strong>NOTE</strong>: When you use pagination, you will not be charged for retrieving subsequent pages of results.</p>
./classify-video.html:<p>Use this method to classify a video based on the entities or actions shown in that video.</p>
./concepts-filtering-search-results.1.html:<p>You can only suggest edits to Markdown body content, but not to the API spec.</p>
./guides-providing-transcriptions-from-the-local-file-system.html:<p>In this guide you&#x27;ll learn how to upload a video and provide a transcription when both files reside on the local file system.</p>
./guides-providing-transcriptions-from-the-local-file-system.html:<p>Note that, although the example code in this guide is written in <a target="_self" href="https://www.python.org/">Python</a> and <a target="_self" href="https://nodejs.org/en/">Node.js</a> the API  is compatible with most programming languages, and you can also use <a target="_self" href="https://www.postman.com/">Postman</a> or other REST clients to send requests and view responses.</p>
./guides-providing-transcriptions-from-the-local-file-system.html:<p>Declare the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">/tasks</code> endpoint:</p>
./guides-providing-transcriptions-from-the-local-file-system.html:<p>Read your video file. Open a stream, making sure to replace the placeholders surrounded by <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">&lt;&gt;</code> with your values:</p>
./guides-providing-transcriptions-from-the-local-file-system.html:<p>Read your transcription file. Open a stream, making sure to replace the placeholders surrounded by <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">&lt;&gt;</code> with your values:</p>
./guides-providing-transcriptions-from-the-local-file-system.html:<p>Upload your video and transcription files. Call the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">/tasks</code> endpoint and store the result in a variable named <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">response</code>:</p>
./guides-providing-transcriptions-from-the-local-file-system.html:<p>Store the ID of your task in a variable named <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">TASK_ID</code> and print the status code and response:</p>
./guides-providing-transcriptions-from-the-local-file-system.html:<p><em>(Optional) </em> You can use the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">/tasks/{_id}</code> endpoint to monitor the indexing process. Wait until the status shows as <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">ready</code>:</p>
./guides-providing-transcriptions-from-the-local-file-system.html:<p>The output should look similar to the following one:</p>
./guides-providing-transcriptions-from-the-local-file-system.html:<p>Note</p></h3><p>For details about the possible statuses, see the <a target="_self" href="tasks.html">Tasks</a> page.</p></blockquote></div><div id="updated-at"><p class="DateLine "><i class="icon icon-watch"></i>Updated<!-- --> <!-- -->19 days ago<!-- --> </p>
./uploading-videos.html:<p>Twelve Labs offers the following options for uploading videos:</p>
./uploading-videos.html:<p>Depending on your use case, proceed to one of the following sections:</p>
./reference.html:<p>Use the Twelve Labs Video Understanding API to extract information from your videos and make it available to your applications. The API is organized around REST and returns responses in the JSON format. It is compatible with most programming languages, and you can also use Postman or other REST clients to send requests and view responses.</p>
./reference.html:<p>To call an endpoint, you must construct a URL similar to the following one:</p>
./reference.html:<p><button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">{Method} {BaseURL}/{version}/{resource}/{path_parameters}?{query_parameters}</code></p>
./reference.html:<p>The list below describes each component of a request:</p>
./reference.html:<p>Note that the API requires you to pass a header parameter containing your API key to authenticate each request. For details, see the <a target="_self" href="guides-authentication.html">Authentication</a> page.</p>
./reference.html:<p>Twelve Labs Video Understanding API follows the <a target="_self" href="https://www.rfc-editor.org/rfc/rfc9110.html">RFC 9110</a> standard to indicate the success or failure of a request. Each response contains a field named <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">X-Api-Version</code> that indicates the version of the API against which the operation was performed.</p>
./reference.html:<p>The following list is a summary of the HTTP status codes returned by the API:</p>
./reference.html:<p>HTTP status codes in the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">4xx</code> range indicate an error caused by the parameters you provided in the request. For each error, the API service returns the following fields in the body of the response:</p>
./reference.html:<p>For more details, see the <a target="_self" href="error-codes.html">Error codes</a> page.</p>
./guides-providing-transcriptions-from-urls.1.html:<p>You can only suggest edits to Markdown body content, but not to the API spec.</p>
./guides-multiple-search-options.html:<p>This section provides examples demonstrating how to perform searches by specifying multiple search options. When you specify multiple search options, you can use the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">operator</code> parameter to broaden or narrow your search.</p>
./guides-multiple-search-options.html:<p>The following logical operators are supported:</p>
./guides-multiple-search-options.html:<p>The diagrams below explain the difference between specifying the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">OR</code> and <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">AND</code> logical operators:</p>
./guides-multiple-search-options.html:<p>Note that, for each matching video fragment, the API service returns, among other information, the following fields:</p>
./guides-multiple-search-options.html:<p><button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">modules</code>: An array where each element is a dictionary composed of the following key-value pairs:</p>
./guides-multiple-search-options.html:<p>The following example shows that a video fragment matched your search terms using two types of search - <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">visual</code> and <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">conversation</code>:</p>
./guides-multiple-search-options.html:<p><button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">metadata</code>: An array of objects where each object contains details about the type of search that matched your search terms and is composed of the following key-value pairs:</p>
./guides-multiple-search-options.html:<p>The following example shows that a video fragment matched your search terms  using two types of search - <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">conversation</code> and <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">visual</code>:</p>
./guides-multiple-search-options.html:<p>For a description of each field in the response, see the <a target="_self" href="make-search-request.html">Make a Search Request</a> page.</p>
./guides-multiple-search-options.html:<p>Notes</p>
./guides-multiple-search-options.html:<p>Note that, although the example code in this guide is written in <a target="_self" href="https://www.python.org/">Python</a> and <a target="_self" href="https://nodejs.org/en/">Node.js</a>, the API  is compatible with most programming languages, and you can also use <a target="_self" href="https://www.postman.com/">Postman</a> or other REST clients to send requests and view responses.</p>
./guides-multiple-search-options.html:<p>The examples in the following sections assume the following:</p>
./guides-multiple-search-options.html:<p>The following example combines the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">visual</code> and <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">conversation</code> search options, using the logical <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">OR</code> operator to specify that the API should return the results for which any search option matches:</p>
./guides-multiple-search-options.html:<p>The following example output was truncated for brevity:</p>
./guides-multiple-search-options.html:<p>The following example combines  the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">visual</code> and <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">conversation</code> search options, using the logical <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">AND</code> operator to specify that the API should return only the results for which all search options match:</p>
./guides-multiple-search-options.html:<p>The following example output was truncated for brevity:</p>
./guides-multiple-search-options.html:<p>The following example combines the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">conversation</code> and <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">text_in_video</code>search options, using the logical <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">OR</code> operator to specify that the API should return the results for which any search option matches:</p>
./guides-multiple-search-options.html:<p>The following example output was truncated for brevity:</p>
./guides-multiple-search-options.html:<p>The following example combines the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">convesation</code> and <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">text_in_video</code> search options, using the logical <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">AND</code> operator to specify that the API should return only the results for which all search options match:</p>
./guides-multiple-search-options.html:<p>The following example output was truncated for brevity:</p>
./guides-multiple-search-options.html:<p>The following example combines the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">conversation</code>, <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">text_in_video</code>, and <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">visual</code> search options, using the logical <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">OR</code> operator to specify that the API should return the results for which any search option matches:</p>
./guides-multiple-search-options.html:<p>The following example output was truncated for brevity:</p>
./guides-multiple-search-options.html:<p>The following example combines the<button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">conversation</code>, <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">text_in_video</code>, and <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">visual</code> search options, using the logical <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">AND</code> operator to specify that the API should return only the results for which all search options match:</p>
./guides-multiple-search-options.html:<p>The following example output was truncated for brevity:</p>
./guides-multiple-search-options.html:<p>This endpoint supports pagination. For details see the <a target="_self" href="pagination-search.html">Pagination &gt; Search</a> page.</p>
./guides-multiple-search-options.html:<p>This endpoint supports filtering. For details, see the <a target="_self" href="concepts-filtering-search-results.html">Filter search results based on metadata</a> page.</p>
./guides-multiple-search-options.html:<p>The <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">search</code> endpoint allows you to group or ungroup your search results based on the unique identifiers of the videos. For details, see the <a target="_self" href="grouping-and-ungrouping-items-in-a-response.html">Grouping and ungrouping items in a response</a> page.</p>
./classifying-the-content-of-a-single-video.1.html:<p>You can only suggest edits to Markdown body content, but not to the API spec.</p>
./grouping-and-ungrouping-items-in-a-response.html:<p>Grouping and ungrouping refer to the process of organizing the results of a search query in a specific way. The API service allows for grouping based on the unique identifiers of the videos. For example, this is useful when building a user interface, because it allows your users to better understand and navigate the search results. On the other hand, ungrouping presents your search results in a flat list. This is useful if you want to view all of the search results in a simple manner.</p>
./grouping-and-ungrouping-items-in-a-response.html:<p>To group or ungroup items in a response, use the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">group_by</code> parameter, specifying one of the following values:</p>
./grouping-and-ungrouping-items-in-a-response.html:<p>Note</p></h3><p>The <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">group_by</code> parameter is optional and its default value is <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">clip</code>. If omitted, the API service will use the default value. For clarity, the examples in this section always specify the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">group_by</code> parameter.</p>
./grouping-and-ungrouping-items-in-a-response.html:<p>The following example code searches for car accidents, and the matching video fragments in the response are grouped by video:</p>
./grouping-and-ungrouping-items-in-a-response.html:<p>The following output has been truncated for brevity:</p>
./grouping-and-ungrouping-items-in-a-response.html:<p>In the example output above, note that the  <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">data</code> array contains a list of objects. Each object corresponds to a video that matches your query and is composed of the following key-value pairs:</p>
./grouping-and-ungrouping-items-in-a-response.html:<p>The following example code searches for car accidents, and the matching video fragments in the response are not grouped:</p>
./grouping-and-ungrouping-items-in-a-response.html:<p>The following output was truncated for brevity:</p>
./grouping-and-ungrouping-items-in-a-response.html:<p>In the example output above, note that the  <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">data</code> array contains a list of objects, and each object has a property named  <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">video_id</code>, which represents the unique identifier of the video that matched your query.</p></div><div id="updated-at"><p class="DateLine "><i class="icon icon-watch"></i>Updated<!-- --> <!-- -->8 days ago<!-- --> </p>
./concepts-search-engines.1.html:<p>You can only suggest edits to Markdown body content, but not to the API spec.</p>
./providing-transcriptions-cloud-to-cloud-transfers.html:<p>Cloud-to-cloud transfers allow you to group multiple video indexing operations in a single API request. For details about this feature, see the <a target="_self" href="guides-upload-videos-cloud-to-cloud-transfers.html">Cloud-to-cloud transfers</a> page.</p>
./providing-transcriptions-cloud-to-cloud-transfers.html:<p>The <a target="_self" href="https://api.twelvelabs.io/dashboard">Dashboard</a> page allows you to download the list of videos in your AWS S3 bucket as a JSON, and you can provide transcriptions by editing this file in a plain-text editor.</p>
./providing-transcriptions-cloud-to-cloud-transfers.html:<p>Open the JSON file containing the videos you want to upload and update the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">transcription</code> fields by adding the S3 URLs of your transcription files. The following example adds two transcription files: <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">01.srt</code> and <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">02.srt</code>:</p>
./providing-transcriptions-cloud-to-cloud-transfers.html:<p>Declare the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">/tasks/transfers</code> endpoint:</p>
./providing-transcriptions-cloud-to-cloud-transfers.html:<p>Read your JSON file. Open a stream making sure to replace the placeholders surrounded by <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">&lt;&gt;</code> with your values:</p>
./providing-transcriptions-cloud-to-cloud-transfers.html:<p>If you&#x27;re using Python, store the file to upload in an array named <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">file_param</code> and specify that you want to make a <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">multipart/form-data</code> request. If you&#x27;re using Node.js, store the file to upload in a variable named <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">formData</code> of type <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">FormData</code>:</p>
./providing-transcriptions-cloud-to-cloud-transfers.html:<p>Upload the JSON file. Call the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">/tasks/transfers</code> endpoint, store the result in a variable named <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">response</code>, and print the status code and the response:</p>
./guides-extracting-transcriptions.html:<p>At a high level, extracting a transcription from a video involves the following steps:</p>
./guides-extracting-transcriptions.html:<p>Note that, although the example code in this guide is written in <a target="_self" href="https://www.python.org/">Python</a> and <a target="_self" href="https://nodejs.org/en/">Node.js</a>, the API  is compatible with most programming languages, and you can also use <a target="_self" href="https://www.postman.com/">Postman</a> or other REST clients to send requests and view responses.</p>
./guides-extracting-transcriptions.html:<p>Construct the URL for extracting the transcription based on the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">INDEX_ID</code> and <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">VIDEO_ID</code> variables:</p>
./guides-extracting-transcriptions.html:<p>Retrieve the transcription and print it out to the console:</p>
./guides-extracting-transcriptions.html:<p>The following example output was truncated for brevity:</p>
./guides-extracting-transcriptions.html:<p>Note that you can use the start and end query parameters to specify the time range for which you want to retrieve the transcription. The following example URL retrieves the transcription for the first 10 seconds of the video:</p>
./versioning.1.html:<p>You can only suggest edits to Markdown body content, but not to the API spec.</p>
./guides-extracting-information-from-videos.1.html:<p>You can only suggest edits to Markdown body content, but not to the API spec.</p>
./list-videos.html:<p>This method returns a list of the videos in the specified index. If no parameters are specified, the API returns your videos sorted by creation date, with the newest at the top of the list.</p>
./tasks-transfers.html:<p>Use the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">/tasks/transfers</code> endpoint to upload multiple videos at once by grouping several video indexing operations in a single API call. The request body must specify a JSON file that contains an array. Each element of the array represents a file to be uploaded and indexed and has the following fields:</p>
./guides-authentication.html:<p>The API uses keys for authentication, and your key is similar to a password. Avoid distributing your API key to other users, or hard-coding it. Generate a new key whenever you think your account may have been compromised.</p>
./guides-authentication.html:<p>For security reasons, your API key automatically expires every two months. When your key has expired, you must generate a new one to continue using the API.</p>
./guides-authentication.html:<p>Notes</p>
./guides-authentication.html:<p>The process of managing your API key can be divided into the following tasks:</p>
./guides-authentication.html:<p>The example code in this section assumes the following:</p>
./guides-authentication.html:<p>The example creates a new index by calling the POST method of the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">/indexes</code> endpoint. Note that, every time you make a request, you must pass the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">x-api-key</code> header parameter with the value of your API key:</p>
./guides-authentication.html:<p>If your key has expired or is invalid, the response should look similar to the following:</p>
./cloud-to-cloud-transfer.html:<p>This method makes a cloud-to-cloud transfer.</p>
./frequently-asked-questions.html:<p>To list all the videos in an index, call the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">GET</code> method of the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">/indexes/videos</code> endpoint, replacing <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">&lt;YOUR_INDEX_ID&gt;</code> with the unique identifier of your index:</p>
./frequently-asked-questions.html:<p>The output should look similar to the following one. Note that, in the example output below, two videos named <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">01.mp4</code> and <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">02.mp4</code> are being stored in the index:</p>
./frequently-asked-questions.html:<p>You can specify multiple search options in conjunction with the operator parameter to broaden or narrow your search. For details and examples, see the <a target="_self" href="guides-multiple-search-options.html">Multiple search options</a> page.</p></div><div id="updated-at"><p class="DateLine "><i class="icon icon-watch"></i>Updated<!-- --> <!-- -->2 days ago<!-- --> </p>
./guides-retrieving-thumbnails.html:<p>To help users quickly preview videos, the API service allows you to retrieve thumbnails of your videos that you can display in your application.</p>
./guides-retrieving-thumbnails.html:<p>You can retrieve thumbnails when:</p>
./guides-retrieving-thumbnails.html:<p>Note the following about retrieving thumbnails:</p>
./guides-retrieving-thumbnails.html:<p>The steps for using retrieving are as follows:</p>
./guides-retrieving-thumbnails.html:<p>Note that, although the example code in this guide is written in <a target="_self" href="https://www.python.org/">Python</a> and <a target="_self" href="https://nodejs.org/en/">Node.js</a> the API  is compatible with most programming languages, and you can also use <a target="_self" href="https://www.postman.com/">Postman</a> or other REST clients to send requests and view responses.</p>
./guides-retrieving-thumbnails.html:<p>To create an index and enable thumbnail generation for it, the steps are similar to those described in the <a target="_self" href="quickstart.html#create-an-index">Quickstart &gt; Create an index</a> section, with the following exception: in the body of the request, you must set the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">addons</code> parameter to <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">[&quot;thumbnail&quot;]</code>.</p>
./guides-retrieving-thumbnails.html:<p>The following example code shows how you can create an index and enable thumbnail generation for it. Make sure to replace the placeholder surrounded by <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">&lt;&gt;</code> with the name of your index.</p>
./guides-retrieving-thumbnails.html:<p>In the example code above, the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">addons</code> field of the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">data</code> dictionary enables thumbnail generation for this index.</p>
./guides-retrieving-thumbnails.html:<p>The output should look similar to the following one:</p>
./guides-retrieving-thumbnails.html:<p>Once you&#x27;ve created an index and enabled thumbnail generation for it, you can upload videos to it.</p>
./guides-retrieving-thumbnails.html:<p>The following example code shows how you can upload a video. Make sure to replace the placeholders surrounded by <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">&lt;&gt;</code> with your values.</p>
./guides-retrieving-thumbnails.html:<p>The output should look similar to the following one:</p>
./guides-retrieving-thumbnails.html:<p>Use the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">/tasks/{_id}</code> endpoint to monitor the indexing process. Wait until the status shows as ready:</p>
./guides-retrieving-thumbnails.html:<p>Store the unique identifier of your video in a variable named <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">VIDEO_ID</code> and print it:</p>
./guides-retrieving-thumbnails.html:<p>The output should look similar to the following one:</p>
./guides-retrieving-thumbnails.html:<p>Note that you can also use webhooks to monitor the status of the indexing process. For details, see the <a target="_self" href="guides-using-webhooks.html">Using webhooks</a> section.</p>
./guides-retrieving-thumbnails.html:<p>There are two ways to retrieve thumbnails, depending on your use case:</p>
./guides-retrieving-thumbnails.html:<p>Note that the API service returns a unique URL for each thumbnail, and the URLs expire in one hour.</p>
./guides-retrieving-thumbnails.html:<p>The <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">GET</code> method of the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">/indexes/{index-id}/videos/{video_id}/thumbnail</code> endpoint retrieves a thumbnail of an individual video. By default, when calling this method, the API service retrieves a thumbnail from the middle of the video. Optionally, you can pass the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">time</code> query parameter to specify the time, in seconds, at which the API service must retrieve the thumbnail.</p>
./guides-retrieving-thumbnails.html:<p>The following example code retrieves a thumbnail from the middle of the video by calling the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">/indexes/{index-id}/videos/{video_id}/thumbnail</code> endpoint and passing it the following arguments:</p>
./guides-retrieving-thumbnails.html:<p>The output should look similar to the following one:</p>
./guides-retrieving-thumbnails.html:<p>The following example code retrieves a thumbnail from a specific time by calling the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">/indexes/{index-id}/videos/{video_id}/thumbnail</code> endpoint, and passing it the following parameters:</p>
./guides-retrieving-thumbnails.html:<p>Make sure to replace the placeholder surrounded by <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">&lt;&gt;</code> with your value.</p>
./guides-retrieving-thumbnails.html:<p>When you perform a search on an index for which thumbnail generation is enabled, the API service returns a field named <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">thumbnail_url</code> that contains a thumbnail from the middle of each matching video fragment.</p>
./guides-retrieving-thumbnails.html:<p>The following example code performs a search on an index for which thumbnail generation is enabled. Make sure to replace the placeholder surrounded by <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">&lt;&gt;</code> with your search query.</p>
./guides-retrieving-thumbnails.html:<p>The following example output was truncated for brevity:</p>
./guides-retrieving-thumbnails.html:<p>In this example output, the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">data</code> array contains two objects, and each object has a  property named <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">thumbnail_url</code> containing the URL of the thumbnail for the matching video fragment.</p></div><div id="updated-at"><p class="DateLine "><i class="icon icon-watch"></i>Updated<!-- --> <!-- -->5 days ago<!-- --> </p>
./content-classification.1.html:<p>You can only suggest edits to Markdown body content, but not to the API spec.</p>
./retrieve-ocr.html:<p>This method retrieves text recognized in a video (OCR).</p>
./guides-requirements-for-processing-notifications.html:<p>To receive notifications, ensure that the URL of your endpoint is publicly accessible. Once your application receives notifications, it must perform at least the following steps:</p>
./guides-requirements-for-processing-notifications.html:<p>Twelve Labs includes a unique header named <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">TL-Signature</code> in each <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">POST</code> request. The header is composed of the following parts:</p>
./guides-requirements-for-processing-notifications.html:<p>To validate the signature and verify that the event was sent by Twelve Labs:</p>
./guides-requirements-for-processing-notifications.html:<p>The API service uses a secret key to sign each <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">POST</code> request. You must retrieve it from the <a target="_self" href="https://api.twelvelabs.io/dashboard">Dashboard</a> page by following the steps in the <a target="_self" href="guides-manage-webhooks.html#retrieve-your-secret-key">Retrieve your secret key</a> section and then store it in your application.</p>
./guides-requirements-for-processing-notifications.html:<p>Extract the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">t</code> and <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">v1</code> fields. Split the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">TL-Signature</code> header, using the comma (<button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">,</code>) character as a separator.</p>
./guides-requirements-for-processing-notifications.html:<p>Generate a signed payload. Concatenate the timestamp retrieved from the header and the raw request body, using the dot ( <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">.</code>) character as a separator.  </p>
./guides-requirements-for-processing-notifications.html:<p>Create a <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">HmacSha256</code> signature using the secret key you&#x27;ve retrieved from the <a target="_self" href="https://api.twelvelabs.io/dashboard">Dashboard</a> page for the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">HMAC</code> key and the signed payload as payload.</p>
./guides-requirements-for-processing-notifications.html:<p>The following example shows how you can generate a signature in Go:</p>
./guides-requirements-for-processing-notifications.html:<p>Compare the signature computed in the previous step with the signature provided in the header. If the signatures match, it means that Twelve Labs is the sender of the notification.</p>
./guides-requirements-for-processing-notifications.html:<p><em>(Optional)</em> As an additional security measure, you can also compare the timestamp received in the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">t</code> field to the current time. Twelve Labs suggest you consider valid all the requests for which the time difference is less than five minutes.</p>
./guides-requirements-for-processing-notifications.html:<p>For each notification that the API service sends to your webhook, it must return a <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">2xx</code> status code to indicate that the notification has successfully been delivered. A different response code will be treated by the API service as a failure, and the status will show as <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">Failed</code>.</p></div><div id="updated-at"><p class="DateLine "><i class="icon icon-watch"></i>Updated<!-- --> <!-- -->19 days ago<!-- --> </p>
./quickstart.1.html:<p>You can only suggest edits to Markdown body content, but not to the API spec.</p>
./retrieve-video-information.html:<p>This method retrieves information about the specified video.</p>
./error-codes.html:<p>This page lists the most common error messages you may encounter while using the API service.</p>
./classifying-all-the-videos-within-an-index.1.html:<p>You can only suggest edits to Markdown body content, but not to the API spec.</p>
./guides-single-search-option.1.html:<p>You can only suggest edits to Markdown body content, but not to the API spec.</p>
./guides-webhooks-notification-schema.1.html:<p>You can only suggest edits to Markdown body content, but not to the API spec.</p>
./concepts-search-options.html:<p>Search options specify the source of information the search engine uses when performing a search. The following values are supported:</p>
./concepts-search-options.html:<p>Notes</p>
./concepts-filtering-indexes.1.html:<p>You can only suggest edits to Markdown body content, but not to the API spec.</p>
./concepts-search-engines.html:<p>An engine indexes your videos and allows you to find specific moments in your video library.</p>
./concepts-search-engines.html:<p>While using the API, you interact with a search engine in the following ways:</p>
./concepts-search-engines.html:<p><strong>Creating indexes</strong>: Each engine uses its own family of deep learning models to index videos. When you create an index, you assign it to a search engine and specify how it&#x27;ll process your videos by passing the <a target="_self" href="concepts-indexing-options.html"><button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">index_options</code></a> parameter in the body of the request. These settings apply to all the videos you upload to your index and cannot be changed.</p>
./concepts-search-engines.html:<p><strong>Performing searches</strong>:  When you <a target="_self" href="guides-performing-searches.html">perform a search</a>, you pass the following parameters:</p>
./concepts-search-engines.html:<p><button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">query</code>: Represents your search query. Note that you can use everyday language. The following examples are valid queries: &quot;birds flying near a castle&quot;, &quot;sun shining on water&quot;, &quot;chickens on the road&quot;, &quot;an officer holding a child&#x27;s hand.&quot;, &quot;crowd cheering in the stadium.&quot;</p>
./concepts-search-engines.html:<p><button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">search_options</code>: Indicate the source of information the search engine uses to perform a search. This parameter can take one or more of the following values: <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">visual</code>, <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">conversation</code>, <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">text_in_video</code>. For details, see the <a target="_self" href="concepts-search-options.html">Search options</a> page.</p>
./concepts-search-engines.html:<p><button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">conversation_option</code>: Indicates whether the search engine should return the results that are semantically similar to your search query or it should return only the exact matches.</p>
./concepts-search-engines.html:<p>The search engine uses these parameters to find the moments in your videos that match your requirements and returns an array of objects as described in the <a target="_self" href="make-search-request.html">Make a search request</a> section.</p>
./concepts-search-engines.html:<p>To handle different use cases and to improve the performance of the API service, Twelve Labs has developed the search engines described in the sections below.</p>
./concepts-search-engines.html:<p>The second version of the Marengo search engine offers performance improvements over the first version. Its state-of-the-art AI technology provides a 70% relative performance increase when performing visual searches compared to the first version.</p>
./concepts-search-engines.html:<p>Note</p></h3><p>Twelve Labs strongly recommends you use <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">marengo2</code>.</p>
./concepts-search-engines.html:<p>Marengo is the search engine that was available when the API service launched. It allows you to find the exact moments in your videos by writing semantic queries in everyday language.</p></div><div id="updated-at"><p class="DateLine "><i class="icon icon-watch"></i>Updated<!-- --> <!-- -->19 days ago<!-- --> </p>
./classify-retrieve-specific-page.html:<p>The <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">/classify/bulk</code> endpoint returns the first page or results. You can use this endpoint to retrieve the rest of the pages.</p>
./concepts-pagination.html:<p>To improve the performance of your application, all Twelve Labs API endpoints that return a list of results support pagination, but the implementation is slightly different depending on the endpoint.</p>
./concepts-pagination.html:<p>For details, proceed to one of the following pages:</p>
./retrieve-video-indexing-task.html:<p>This method retrieves a video indexing task.</p>
./content-classification.html:<p>Content classification is the process of organizing content into distinct categories based on specific criteria. This process helps you organize your videos into more manageable and useful fragments, making them easier to find, access, and use.</p>
./content-classification.html:<p>If you have an extensive library of videos you&#x27;d like to analyze to identify specific actions or entities, manually finding the segments that meet your criteria is time-consuming. With a single API call, the Twelve Labs Video Understanding API automatically identifies the actions and entities you specify whenever they appear in your videos and assigns a likelihood score that indicates how likely those actions or entities are to occur in each of your videos.</p>
./content-classification.html:<p>Depending on your use case, proceed to one of the following sections:</p>
./frequently-asked-questions.1.html:<p>You can only suggest edits to Markdown body content, but not to the API spec.</p>
./guides-extracting-text-in-videos-ocr.1.html:<p>You can only suggest edits to Markdown body content, but not to the API spec.</p>
./concepts-filtering.1.html:<p>You can only suggest edits to Markdown body content, but not to the API spec.</p>
./guides-manage-webhooks.html:<p>Before you can receive notifications, you must first register your endpoint by following the steps below:</p>
./guides-manage-webhooks.html:<p>When a webhook is no longer needed, you can remove it.</p>
./guides-manage-webhooks.html:<p>This section explains how you can temporarily suspend and then reactivate notifications by disabling or enabling a webhook</p>
./guides-manage-webhooks.html:<p>Twelve Labs signs the notifications it sends to your endpoint by including a signature in each notification. Before you can verify signatures, you must retrieve your secret key from the <strong>Settings</strong> section of the <a target="_self" href="https://api.twelvelabs.io/dashboard">Dashboard</a> page.</p>
./make-search-request.html:<p>Use the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">/search</code> endpoint to search for relevant matches in an index. This endpoint supports sorting, pagination and filtering.</p>
./make-search-request.html:<p>This endpoint returns the first page of results. To retrieve the subsequent pages, you must call the GET method of the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">/search/{page-token}</code> endpoint, passing it the unique identifier of the page you want to retrieve.</p>
./make-search-request.html:<p><strong>NOTE</strong>: When you use pagination, you will not be charged for retrieving subsequent pages of results.</p>
./video-indexing-tasks-by-status.html:<p>This method retrieves the number video indexing tasks in each status for the specified index.</p>
./understand.1.html:<p>You can only suggest edits to Markdown body content, but not to the API spec.</p>
./guides-upload-videos-cloud-to-cloud-transfers.1.html:<p>You can only suggest edits to Markdown body content, but not to the API spec.</p>
./guides-retrieving-thumbnails.1.html:<p>You can only suggest edits to Markdown body content, but not to the API spec.</p>
./classifying-the-content-of-a-single-video.html:<p>This section shows how to classify the content of a single video by calling the POST method of the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">/classify</code> endpoint. For a description of each field in the request and response, see the <a target="_self" href="classify.html">API Reference &gt; Classify</a> page.</p>
./classifying-the-content-of-a-single-video.html:<p>The examples in the following sections assume the following:</p>
./classifying-the-content-of-a-single-video.html:<p>Note that, although the example code in this guide is written in <a target="_self" href="https://www.python.org/">Python</a> and <a target="_self" href="https://nodejs.org/en/">Node.js</a>, the API is compatible with most programming languages, and you can also use <a target="_self" href="https://www.postman.com/">Postman</a> or other REST clients to send requests and view responses.</p>
./classifying-the-content-of-a-single-video.html:<p>The following example code defines a set of car shapes and uses the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">options</code> parameter to specify that the API service should classify your video based on visual cues:</p>
./classifying-the-content-of-a-single-video.html:<p>The output should look similar to the following one</p>
./classifying-the-content-of-a-single-video.html:<p>You can use the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">threshold</code> parameter to narrow down a response obtained from the API service and retrieve only the most relevant results.  This parameter can take one of the following values:</p>
./classifying-the-content-of-a-single-video.html:<p>The following example code specifies that the minimum similarity score between your label and the matching video fragments shouldn&#x27;t be lower than <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">high</code>:</p>
./classifying-the-content-of-a-single-video.html:<p>The output should look similar to the following one:</p>
./classifying-the-content-of-a-single-video.html:<p>The following example code sets the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">include_clips</code> parameter to <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">true</code> to specify that the API service should retrieve information about each matching video fragment:</p>
./classifying-the-content-of-a-single-video.html:<p>The following example output has been truncated for brevity:</p>
./classifying-the-content-of-a-single-video.html:<p>In the example output above, note that each element in the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">clips</code> array contains information about a single matching video fragment.</p>
./classifying-the-content-of-a-single-video.html:<p>The following example code classifies the content of a video based on two labels - <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">Pets</code> and <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">Wild</code> animals:</p>
./classifying-the-content-of-a-single-video.html:<p>The output should look similar to the following one:</p>
./classifying-the-content-of-a-single-video.html:<p>In the example output above, note that the API service identified both labels in the video. The maximum score is higher, and the duration is longer for the video fragments in which wild animals appear. This means that it is more likely this video contains wild animals rather than pets.</p></div><div id="updated-at"><p class="DateLine "><i class="icon icon-watch"></i>Updated<!-- --> <!-- -->7 days ago<!-- --> </p>
./concepts-filtering-search-results.html:<p>When you perform a search, the API service returns all the relevant matches. Filtering narrows the scope of your query. To filter your search results, add the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">filter</code> parameter to the body of your request.</p>
./concepts-filtering-search-results.html:<p>The <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">filter</code> parameter is of type <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">Object</code> and can contain one or more of the following fields:</p>
./concepts-filtering-search-results.html:<p>To indicate the relationship between a field and its value, you can use the exact match or comparison operators.</p>
./concepts-filtering-search-results.html:<p>The exact match operator matches only the results that equal the value you specify. The syntax is as follows: <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">&lt;field&gt;: &lt;value&gt;</code>.</p>
./concepts-filtering-search-results.html:<p>Use the comparison operators (<button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">lte</code> and <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">gte</code>) to match based on the arithmetic comparison. The syntax is as follows: <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">&lt;field&gt;:{&quot;gte&quot;: &lt;value&gt;, &quot;lte&quot;: &lt;value}</code>.</p>
./concepts-filtering-search-results.html:<p>You can filter on multiple fields by adding the fields you want to filter on to the filter query parameter as shown in the <a target="_self" href="concepts-filtering-search-results.html#filter-on-size-width-and-height">Filter on size, width, and height</a> section below.</p>
./concepts-filtering-search-results.html:<p>The examples in this section assume you&#x27;re familiar with the concepts that are described in the <a target="_self" href="quickstart.html">Quickstart</a> section.</p>
./concepts-filtering-search-results.html:<p>Note that, although the example code in this guide is written in <a target="_self" href="https://www.python.org/">Python</a> and <a target="_self" href="https://nodejs.org/en/">Node.js</a> the API  is compatible with most programming languages, and you can also use <a target="_self" href="https://www.postman.com/">Postman</a> or other REST clients to send requests and view responses.</p>
./concepts-filtering-search-results.html:<p>The following example code uses the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">id</code> field of the filter query parameter to filter on a specific video ID:</p>
./concepts-filtering-search-results.html:<p>The following example output was truncated for brevity:</p>
./concepts-filtering-search-results.html:<p>Note that this query returns only the results found in the specified videos.</p>
./concepts-filtering-search-results.html:<p>The following example code uses the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">id</code> field of the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">filter</code> query parameter to filter on multiple video IDs:</p>
./concepts-filtering-search-results.html:<p>The following example output was truncated for brevity:</p>
./concepts-filtering-search-results.html:<p>Note that this query returns the results found in both videos.</p>
./concepts-filtering-search-results.html:<p>The example code below uses the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">duration </code>field of the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">filter</code> query parameter to return only the matches found in videos that are longer than or equal to <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">700</code> seconds <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">AND</code> shorter than or equal to <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">750</code> seconds:</p>
./concepts-filtering-search-results.html:<p>The output should look similar to the following one:</p>
./concepts-filtering-search-results.html:<p>The example code below uses the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">size</code>, <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">width</code>, and <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">height</code> fields of the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">filter</code> query parameter to return only the matches found in videos that meet all the following criteria:</p>
./concepts-filtering-search-results.html:<p>The following example output was truncated for brevity:</p>
./concepts-filtering-search-results.html:<p>The example code below filters on a custom field named <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">needs_review</code> of type <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">boolean</code>. For details about specifying custom metadata, see the <a target="_self" href="update-video-information.html">Update video information</a> page. The API service will return only the matches found in videos that are tagged with <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">needs_review: true</code>.</p>
./concepts-filtering-search-results.html:<p>The following output was truncated for brevity:</p>
./engines.html:<p>Use the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">/engines</code> endpoint to retrieve details about the available search engines. The <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">engine</code> object has the following elements:</p>
./search.html:<p>Use the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">/search</code> endpoint to search for relevant matches in an index. This endpoint supports pagination and filtering.</p>
./search.html:<p>Note</p></h3><p>When you use pagination, you will not be charged for retrieving subsequent pages of results.</p>
./guides-uploading-videos.html:<p>Twelve Labs offers the following options for uploading videos:</p>
./guides-uploading-videos.html:<p>Depending on your use case, proceed to one of the following sections:</p>
./pagination-search.1.html:<p>You can only suggest edits to Markdown body content, but not to the API spec.</p>
./providing-transcriptions-cloud-to-cloud-transfers.1.html:<p>You can only suggest edits to Markdown body content, but not to the API spec.</p>
./guides-extracting-text-in-videos-ocr.html:<p>At a high level, extracting text recognized in a video (OCR) involves the following steps:</p>
./guides-extracting-text-in-videos-ocr.html:<p>Note that, although the example code in this guide is written in <a target="_self" href="https://www.python.org/">Python</a> and <a target="_self" href="https://nodejs.org/en/">Node.js</a>, the API  is compatible with most programming languages, and you can also use <a target="_self" href="https://www.postman.com/">Postman</a> or other REST clients to send requests and view responses.</p>
./guides-extracting-text-in-videos-ocr.html:<p>Construct the URL for retrieving the text recognized in your video based on the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">INDEX_ID</code> and <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">VIDEO_ID</code> variables:</p>
./guides-extracting-text-in-videos-ocr.html:<p>Retrieve the text and print it out to the console:</p>
./guides-extracting-text-in-videos-ocr.html:<p>The following example output was truncated for brevity:</p>
./guides-extracting-text-in-videos-ocr.html:<p>Note that you can use the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">start</code> and <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">end</code> query parameters to specify the time range for which you want to retrieve the text. The following example URL retrieves the text for the first 10 seconds of the video:</p>
./docs.html:<p>Twelve Labs Video Understanding API is an AI-powered video search solution that extracts information such as movement, objects, sound, text on screen, and speech from your videos and allows you to find specific moments using everyday language.</p>
./docs.html:<p>The following sections describe the key capabilities of Twelve Labs Video Understanding API and compare it to other solutions.</p>
./docs.html:<p>Developers using Twelve Labs Video Understanding API have access to the following key capabilities:</p>
./docs.html:<p>Note</p></h3><p>Twelve Labs is continuously adding new features. See the <a target="_self" href="roadmap.html">Roadmap</a> page for details.</p>
./docs.html:<p>The table below provides a basic comparison between Twelve Labs Video Understanding API and other cloud-based solutions:</p>
./docs.html:<p>Twelve Labs Video Understanding API compared to other solutions</p>
./roadmap.html:<p>This page outlines the roadmap for Twelve Labs Video Understanding API. Note that Twelve Labs is continuously adding new features based on customer feedback. As a result, this roadmap is not exhaustive and is subject to change.</p>
./sorting.html:<p>Sorting allows you to arrange returned data in a chosen order. The <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">GET</code> method of the  <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">/indexes</code>, <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">/indexes/videos</code>, and <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">/tasks</code> endpoints support sorting.</p>
./sorting.html:<p>You can sort the results of an API query by specifying the sorting criteria and direction as query parameters. To separate the endpoint and the query parameters, you must use a <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">?</code> symbol. If you add both sorting criteria and direction, you must separate them using the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">&amp;</code> symbol.</p>
./sorting.html:<p>Use the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">sort_by</code> parameter to specify the field you want to sort on. The following options are available:</p>
./sorting.html:<p>Use the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">sort_option</code> parameter to specify the sorting direction. The following options are available:</p>
./sorting.html:<p>The examples in this section are specific to using the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">/tasks</code> endpoint. However, the principles demonstrated are similar when using the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">/indexes</code> and <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">/indexes/videos</code> endpoints.</p>
./sorting.html:<p>The examples in this section assume you&#x27;re familiar with the concepts that are described in the <a target="_self" href="quickstart.html">Quickstart</a> section.</p>
./sorting.html:<p>Note that, although the example code in this guide is written in <a target="_self" href="https://www.python.org/">Python</a> and <a target="_self" href="https://nodejs.org/en/">Node.js</a> the API  is compatible with most programming languages, and you can also use <a target="_self" href="https://www.postman.com/">Postman</a> or other REST clients to send requests and view responses.</p>
./sorting.html:<p>The following example code sorts items by creation date, using the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">sort_by</code> query parameter:</p>
./sorting.html:<p>The following example output was truncated for brevity:</p>
./sorting.html:<p>In this example output, note that the default sort order is descending. The most recent items are displayed first at the top of the list.</p>
./sorting.html:<p>The following example code sorts items by creation date and reverses the sorting order:</p>
./sorting.html:<p>The following example output was truncated for brevity:</p>
./sorting.html:<p>The following example code sorts items by creation date and specifies that each page should contain <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">5</code> items:</p>
./sorting.html:<p>The following example output was truncated for brevity:</p>
./pagination-content-classification.html:<p>When using the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">/classify/bulk</code> endpoint, pagination works in a similar fashion to the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">/search</code> endpoint. For details, see the <a target="_self" href="pagination-search.html">Pagination &gt; Search</a> page.</p></div><div id="updated-at"><p class="DateLine "><i class="icon icon-watch"></i>Updated<!-- --> <!-- -->18 days ago<!-- --> </p>
./guides-using-webhooks.1.html:<p>You can only suggest edits to Markdown body content, but not to the API spec.</p>
./concepts-search-options.1.html:<p>You can only suggest edits to Markdown body content, but not to the API spec.</p>
./guides-combined-queries.1.html:<p>You can only suggest edits to Markdown body content, but not to the API spec.</p>
./retrieve-index.html:<p>This method retrieves details about the specified index.</p>
./introduction.1.html:<p>You can only suggest edits to Markdown body content, but not to the API spec.</p>
./introduction.html:<p>Twelve Labs Video Understanding API is an AI-powered video search solution that extracts information such as movement, objects, sound, text on screen, and speech from your videos and allows you to find specific moments using everyday language.</p>
./introduction.html:<p>The following sections describe the key capabilities of Twelve Labs Video Understanding API and compare it to other solutions.</p>
./introduction.html:<p>Developers using Twelve Labs Video Understanding API have access to the following key capabilities:</p>
./introduction.html:<p>Note</p></h3><p>Twelve Labs is continuously adding new features. See the <a target="_self" href="roadmap.html">Roadmap</a> page for details.</p>
./introduction.html:<p>The table below provides a basic comparison between Twelve Labs Video Understanding API and other cloud-based solutions:</p>
./introduction.html:<p>Twelve Labs Video Understanding API compared to other solutions</p>
./guides-manage-webhooks.1.html:<p>You can only suggest edits to Markdown body content, but not to the API spec.</p>
./sorting.1.html:<p>You can only suggest edits to Markdown body content, but not to the API spec.</p>
./concepts-indexing-options.html:<p>Indexing options determine how a search engine processes your videos. You must specify the indexing options when you create a new index, and you cannot modify them once the index has been created. The following indexing options are available:</p>
./concepts-indexing-options.html:<p>Note</p></h3><p>The <a target="_self" href="concepts-search-options.html">search options</a> you use when performing a search must be a subset of the indexing options specified when the index was created.</p></blockquote></div><div id="updated-at"><p class="DateLine "><i class="icon icon-watch"></i>Updated<!-- --> <!-- -->2 days ago<!-- --> </p>
./list-indexes.html:<p>This method returns a list of the indexes in your account. The API returns indexes sorted by creation date, with the oldest indexes at the top of the list.</p>
./classifying-all-the-videos-within-an-index.html:<p>This section shows how to classify the content of all the videos within an index by calling the POST method of the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">/classify/bulk</code> endpoint. For a description of each field in the request and response, see the <a target="_self" href="classify-bulk.html">API Reference &gt; Classify all the videos in an index</a> page.</p>
./classifying-all-the-videos-within-an-index.html:<p>The examples in the following sections assume the following:</p>
./classifying-all-the-videos-within-an-index.html:<p>Note that, although the example code in this guide is written in <a target="_self" href="https://www.python.org/">Python</a> and <a target="_self" href="https://nodejs.org/en/">Node.js</a>, the API is compatible with most programming languages, and you can also use <a target="_self" href="https://www.postman.com/">Postman</a> or other REST clients to send requests and view responses.</p>
./classifying-all-the-videos-within-an-index.html:<p>The following example code defines a set of car shapes and uses the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">options</code> parameter to specify that the API service should classify your videos based on visual cues:</p>
./classifying-all-the-videos-within-an-index.html:<p>The following output has been truncated for brevity:</p>
./classifying-all-the-videos-within-an-index.html:<p>In the example output above, note that the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">data</code> array is composed of three objects. Each object contains, among other information, a field named <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">video_id</code>representing the unique identifier of the video that has been classified.</p>
./classifying-all-the-videos-within-an-index.html:<p>You can use the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">threshold</code> parameter to narrow down a response obtained from the API service and retrieve only the most relevant results. This parameter can take one of the following values:</p>
./classifying-all-the-videos-within-an-index.html:<p>The following example code specifies that the minimum similarity score between your label and the matching video fragments shouldn&#x27;t be lower than <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">high</code>:</p>
./classifying-all-the-videos-within-an-index.html:<p>The output should look similar to the following one:</p>
./classifying-all-the-videos-within-an-index.html:<p>The following example code sets the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">include_clips</code> parameter to true to specify that the API service should retrieve information about each matching video fragment:</p>
./classifying-all-the-videos-within-an-index.html:<p>The following example output has been truncated for brevity:</p>
./classifying-all-the-videos-within-an-index.html:<p>In the example output above, note that each element in the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">data</code> array contains information about a single video.</p>
./classifying-all-the-videos-within-an-index.html:<p>The following example code classifies the content of your videos based on two labels - <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">Pets</code> and <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">Wild animals</code>:</p>
./classifying-all-the-videos-within-an-index.html:<p>The output should look similar to the following one:</p>
./classifying-all-the-videos-within-an-index.html:<p>In the example output above, note that the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">data</code> array contains three objects, each corresponding to a different video. For each video, the response contains information that helps you determine the likelihood that each of the labels you&#x27;ve specified in the request appears in that video.</p>
./classifying-all-the-videos-within-an-index.html:<p>When using the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">/classify/bulk</code> endpoint, pagination works in a similar fashion to the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">/search</code> endpoint. For details, see the <a target="_self" href="pagination-search.html">Pagination &gt; Search</a> page.</p></div><div id="updated-at"><p class="DateLine "><i class="icon icon-watch"></i>Updated<!-- --> <!-- -->7 days ago<!-- --> </p>
./pagination-content-classification.1.html:<p>You can only suggest edits to Markdown body content, but not to the API spec.</p>
./guides-providing-transcriptions-from-the-local-file-system.1.html:<p>You can only suggest edits to Markdown body content, but not to the API spec.</p>
./concepts-filtering-indexes.html:<p>When you have a large number of results, it is useful to fine-tune your requests using filters. The <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">GET</code> method of the  <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">/indexes</code>, <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">/indexes/videos</code>, and<button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">/tasks</code> endpoints support filtering.</p>
./concepts-filtering-indexes.html:<p>To filter the results of an API query, you must specify one or more conditions after the endpoint in the URL of the call. To separate the endpoint and the conditions, you must use a <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">?</code> symbol. If you add multiple conditions, you must separate them using the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">&amp;</code> symbol.</p>
./concepts-filtering-indexes.html:<p>A condition consists of an expression that specifies:</p>
./concepts-filtering-indexes.html:<p>To indicate the relationship between a field and its value, you can use the exact match or comparison operators.</p>
./concepts-filtering-indexes.html:<p>Use the exact match operator (<button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">=</code>) to match only the results that equal the value you specify. The syntax is as follows: <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">&lt;field&gt;=&lt;value&gt;</code>.</p>
./concepts-filtering-indexes.html:<p>Use the comparison operators (<button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">[lte]=</code> and <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">[gte]=</code>) to match based on the arithmetic comparison. The syntax is as follows <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">&lt;field&gt;[lte]=&lt;value&gt;</code> and <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">&lt;field&gt;[gte]=&lt;value&gt;</code>.</p>
./concepts-filtering-indexes.html:<p>You can add multiple filters to a single query. For example, if you want to filter all the video indexing tasks created on a specific date and whose status is pending, you must specify both the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">created_at</code> and <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">status</code> fields in the query parameters, as shown in this example: <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">created_at=2022-05-17&amp;status=pending</code>.</p>
./concepts-filtering-indexes.html:<p>Most of the examples in this section are specific to using the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">/tasks</code> endpoint. However, the principles demonstrated are similar when using the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">/indexes</code> and <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">/indexes/videos</code> endpoints.</p>
./concepts-filtering-indexes.html:<p>The examples in this section assume you&#x27;re familiar with the concepts that are described in the <a target="_self" href="quickstart.html">Quickstart</a> section.</p>
./concepts-filtering-indexes.html:<p>Note that, although the example code in this guide is written in <a target="_self" href="https://www.python.org/">Python</a> and <a target="_self" href="https://nodejs.org/en/">Node.js</a> the API  is compatible with most programming languages, and you can also use <a target="_self" href="https://www.postman.com/">Postman</a> or other REST clients to send requests and view responses.</p>
./concepts-filtering-indexes.html:<p>Note</p></h3><p>Depending on the endpoint, you can filter on various fields. For details, see the <a target="_self" href="reference.html">API Reference</a> section.</p>
./concepts-filtering-indexes.html:<p>The following example code uses the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">created_at</code> query parameter and the exact match operator to return all the items created on a specific date:</p>
./concepts-filtering-indexes.html:<p>The following example output was truncated for brevity:</p>
./concepts-filtering-indexes.html:<p>To display all the items created in a date range, you must combine the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">lte</code> and <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">gte</code> operators. The following example code returns all the video indexing tasks created between <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">2022-05-15</code> and <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">2022-05-16</code>:</p>
./concepts-filtering-indexes.html:<p>The output should look similar to the following one:</p>
./concepts-filtering-indexes.html:<p>To filter based on a file name, use the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">filename</code> query parameter and the exact match operator as shown in the example below:</p>
./concepts-filtering-indexes.html:<p>To filter based on the duration of a video, use the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">duration</code> query parameter and either the exact match or the comparison operators.</p>
./concepts-filtering-indexes.html:<p>The example below uses the exact match operator to display all the videos of a specific duration (<button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">810</code> seconds):</p>
./concepts-filtering-indexes.html:<p>The output should look similar to the following one:</p>
./concepts-filtering-indexes.html:<p>Note</p></h3><p>The API rounds down the duration of the videos when using the exact match operator to filter based on duration.</p>
./concepts-filtering-indexes.html:<p>The example below uses the comparison operators to filter videos longer than <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">600</code> seconds and shorter than <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">820</code> seconds:</p>
./concepts-filtering-indexes.html:<p>The output should look similar to the following one:</p>
./concepts-filtering-indexes.html:<p>The example code below filters your list of videos on a custom field named <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">needs_review</code> of type <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">boolean</code>. For details about specifying custom metadata, see the <a target="_self" href="update-video-information.html">Update video information</a> page. The API service will return only the videos that are tagged with <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">needs_review: true</code>.</p>
./guides-performing-searches.html:<p>This section provides examples demonstrating how to perform searches. The API service allows you to perform the following type of searches by specifying the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">search_options</code> parameter in the body of the request:</p>
./guides-performing-searches.html:<p>Note</p></h3><p>The search options you specify must be a subset of the <a target="_self" href="concepts-indexing-options.html">indexing options</a> used when you created the index.</p>
./guides-performing-searches.html:<p>The API service allows you to specify one or multiple search options. When you specify multiple search options, you can also use the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">AND</code> and <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">OR</code> logical operators to narrow or broaden your search.</p>
./guides-performing-searches.html:<p>Additionally, the <button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">1.1</code> version of the API service introduces a new format of search queries named combined queries. A combined query includes any number of search queries linked with any number of logical operators.</p>
./guides-performing-searches.html:<p>For details, proceed to one of the following pages:</p>
./combined-queries.html:<p>A combined query includes any number of search queries linked with any number of logical operators.</p>
./combined-queries.html:<p>This feature is in beta. For clarity, it is documented in this section. However, note that the /<button aria-label="Copy Code" class="rdmd-code-copy fa"></button><code class="rdmd-code lang- theme-light" data-lang="" name="">search</code> endpoint supports all the features of the /beta/search endpoint.</p>
./combined-queries.html:<p>This endpoint supports pagination and filtering.</p>
./combined-queries.html:<p>Note</p></h3><p>When you use pagination, you will not be charged for retrieving subsequent pages of results.</p>
./roadmap.1.html:<p>You can only suggest edits to Markdown body content, but not to the API spec.</p>
